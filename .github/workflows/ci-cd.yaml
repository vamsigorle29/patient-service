name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Test syntax
      run: |
        python -m py_compile app.py
        python -m py_compile models.py
        python -m py_compile database.py
    
    - name: Test imports
      run: |
        python -c "from app import app; from models import Patient; from database import get_db; print('Imports OK')"
    
    - name: Test health endpoint
      run: |
        python app.py &
        sleep 5
        curl -f http://localhost:8001/health || exit 1
        pkill -f "python app.py"

  build:
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Build Docker image
      run: |
        docker build -t patient-service:latest .
        docker images | grep patient-service
    
    - name: Test Docker image
      run: |
        echo "Testing Docker image: patient-service:latest"
        docker run -d -p 8001:8001 --name test-patient-service patient-service:latest
        sleep 15
        curl -f http://localhost:8001/health || exit 1
        echo "✅ Docker image works correctly"
        docker stop test-patient-service || true
        docker rm test-patient-service || true

